ssh to server
sudo su
apt update && apt upgrade
hostnamectl set-hostname <add hostname>
nano /etc/hosts -> opens hostfile -> under 127.0.0.1 localhost ->
public_ip_address->TAB->added hostname
##firewall
sudo apt install ufw (uncomplicated firewall)
sudo ufw default allow outgoing
sudo ufw default deny incoming
sudo ufw allow ssh
sudo ufw allow 5000
sudo ufw enable -> yes
sudo ufw status
pip freeze > requirements.txt
git clone -> git repo url
ls (confirm folder is there)
sudo apt install python3-pip
sudo apt install python3-venv
python3 -m venv Name_of_folder_with_project/venv
cd Name_of_folder_with_project/ -> ls
source venv/bin/activate
pip install -r requirements.txt (or directory)
(create config file)
sudo touch /etc/config.json
sudo nano /etc/config.json
{
    "MONGO_URI"="mongodb+srv://arek:metabaron@nodeexpressprojects.wegqz.mongodb.net/?retryWrites=true&w=majority",
    "DB_NAME"="shortcut_bugs",
    "DB_COLLECTION" = "bug_tickets"
}
in init file -> import json -> with open('/etc/config.json') as config_file:
    config = json.load(config_file)

mongo = pymongo.MongoClient(config.get['MONGO_URI']) etc

export FLASK_APP = run.py
flask run --host=0.0.0.0